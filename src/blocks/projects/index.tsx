import {Heading} from "../../components/heading";
import {Block} from "../../components/block";

export const Projects = () => {
    return <Block id={"projects"}>
        <Heading variant={"h1"}>Проекты</Heading>
    </Block>
}