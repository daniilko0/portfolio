import {Heading} from "../../components/heading";
import {Block} from "../../components/block";

export const Skills = () => {
    return <Block className={"section"}>
        <Heading variant={"h1"}>Навыки</Heading>
    </Block>
}