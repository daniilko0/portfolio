import styled from "@emotion/styled";

export const MenuItem = styled.span(() => ({
    fontFamily: ["Fira Code", "monospace"],
    fontSize: "24px",
    color: "black",
    textDecoration: "none",
    padding: "0px 10px",
    userSelect: "none",
    cursor: "pointer"
}))